<section class="simple-hero">
    <div class="wrap">
        <div class="centered">
            <h1>{{ block.heading }}</h1>
            {% if block.subheading %}
                <div class="subheading">{{ block.subheading | markdown | safe }}</div>
            {% endif %}
        </div>
    </div>
</section>