<section class="image-hero">
    <img class="overlay" width="1920" height="500" src="{{ block.hero_image }}" alt="{{ block.alt_text }}">
    <div class="centered">
        <h1>{{ block.heading }}<h1>
        {% if block.subheading %}
            {{ block.subheading | markdown | safe }}
        {% endif %}
    </div>
</section>